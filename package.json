{
  "name": "andreasvold-site",
  "version": "1.0.0",
  "private": true,
  "description": "andreasvold.no — Eleventy + Decap CMS, deployed to GitHub Pages",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
